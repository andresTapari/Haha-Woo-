extends Control

var health_var_texture = null


func _ready():
#	health_var_texture = %health_bar
#	health_var_texture.get("texture").set("frames",3)
#	print(health_var_texture.get("texture"))
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta):
	pass
